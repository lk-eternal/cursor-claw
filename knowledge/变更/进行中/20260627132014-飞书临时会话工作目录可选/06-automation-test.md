# 飞书临时会话工作目录可选 - 验收记录

> **变更 ID**：`20260627132014-飞书临时会话工作目录可选`
> **阶段**：`/kb-test`（默认不重跑全链路；T5 静态 + 04-review；飞书 E2E 待手工）
> **评审结论引用**：`04-review.md` 通过；§7 遗留 `-dir` 置首含空格路径、MCP `action=launch` 无 `-dir`

## 1、测试策略与范围

| 维度 | 说明 |
|------|------|
| **层级** | **静态契约**（04-review 全量 diff + `tsc --noEmit`）+ **飞书管理员私聊 E2E**；无单元测试/集成测试脚手架 |
| **目标** | 覆盖 `01-proposal` 验收 1–8、`03-tasks` T1–T5、`02-design` §八·（二）工程补充五项 |
| **通过口径** | 代码路径与 02/03 一致（04 已勾）+ `tsc` 无类型错误；运行态 01·1–7 与 §八·(二) 中需通道/Agent 的项由飞书 E2E 清单复验 |
| **与 review 分工** | 04 负责实现与规范；本文负责验收追溯与 E2E 证据占位 |

## 2、局限与未自动化原因

| 未自动化项 | 原因 |
|------------|------|
| 01 验收 1–7（默认/指定/无效目录、反馈、回退、并发） | 依赖飞书 WebSocket、daemon 运行态与本地多项目目录 A/B；无 headless 契约 |
| §八·(二)·3 `/chat` 切换全路径展示 | 需飞书私聊切换 temp 并观察反馈文案 |
| §八·(二)·4 SDK/CLI 双资源 `workspaceDir` | 需分别启用 SDK/CLI Agent 资源并验证目录绑定 |
| §八·(二)·5 工作流 mkdir 无回归 | 需触发工作流节点目录不存在场景 |
| MCP/Daemon `action=launch` 无 `-dir` | 02/T5 已记范围外；非本期阻塞 |
| `auto_test/` 冒烟脚本 | 本期未新增；通道验收以手工清单为主 |
| `-dir` 置首 + 含空格路径 | 04 §4 设计偏差；常见单 token 路径不受影响 |

## 3、验收追溯表

| 来源 | 验收要点 | 验证方式 | 证据类型 | 状态 |
|------|----------|----------|----------|------|
| **T1** | `parseChatNewArgs` 双顺序、缺参用法、`-dir` join | 04 静态 L452 附近 | 代码复核 | ✅ 静态 |
| **T1** | `validateWorkspacePath` 不存在/无权限/非目录/绝对路径 | 04 静态 | 代码复核 | ✅ 静态 |
| **T2** | `launchIndependentAgent` 透传；temp 跳过 mkdir | 04 静态 | 代码复核 | ✅ 静态 |
| **T2** | workflow 仍自动 mkdir；未传目录用 `effectiveWorkspaceDir` | 04 静态 | 代码复核 | ✅ 静态 |
| **T3** | 解析→校验→失败早返回、不 `syncActiveSession` | 04 静态 L514–520 | 代码复核 | ✅ 静态 |
| **T3** | 成功反馈含任务摘要、完整路径、SessionKey | 04 静态 L535–540 | 代码复核 | ✅ 静态 |
| **T3** | 01 验收 1–7 运行态 | 飞书 E2E M1–M7 | 联调 | ⏳ 待联调 |
| **T4** | `/help` 与 `COMMANDS["/chat"]` 三要点 | 04 静态 | 代码复核 | ✅ 静态 |
| **T5** | 01 验收 1–8 + §八·(二) 汇总 | T5 静态 + E2E | 静态/联调 | ⚠️ 静态✅ E2E⏳ |
| **01·1** | 默认目录 A；反馈展示 A；temp 在 A 运行 | 飞书 `/chat new <任务>` | 联调 | ⏳ 待联调 |
| **01·2** | 指定 B≠A；主会话仍为 A；结束后回退 | `/chat new … -dir B` | 联调 | ⏳ 待联调 |
| **01·3** | 无效目录不创建、活跃不变、友好错误 | 不存在路径 / 文件路径 | 联调 | ⏳ 待联调 |
| **01·4** | ≥3 次成功创建均含任务确认与完整路径 | 默认 1 + 指定 2 | 联调 | ⏳ 待联调 |
| **01·5** | 切换、temp 对话、结束回退无回归 | temp 完整生命周期 | 联调 | ⏳ 待联调 |
| **01·6** | 群聊/他人/Settings UI 无变化 | 边界观察 | 联调 N/A | ⏳ 待确认 |
| **01·7** | 连续两 temp 不同目录无串用 | 连建两 temp | 联调 | ⏳ 待联调 |
| **01·8** | 帮助可查三要点 | `/help` 或 `/chat` 用法 | 静态+联调 | ✅ 静态 |
| **§八·(二)·1** | `-dir` 指向文件时友好错误 | 指定文件路径 | 联调 | ⚠️ 静态✅ |
| **§八·(二)·2** | 校验失败活跃路由不变 | 无效 `-dir` 前后对比 | 联调 | ⚠️ 静态✅ |
| **§八·(二)·3** | `/chat ls` basename；切换全路径 | `/chat ls`、`/chat <n>` | 联调 | ⚠️ 静态✅ |
| **§八·(二)·4** | SDK/CLI 双资源 workspaceDir | 分别创建 temp | 联调 | ⏳ 待联调 |
| **§八·(二)·5** | 工作流 mkdir 无回归 | 工作流不存在节点目录 | 联调 | ⏳ 待联调 |

## 4、场景摘要

### 4.1 飞书 E2E 清单（待手工）

| 场景 ID | 前置 | 步骤摘要 | 期望 | 关联 |
|---------|------|----------|------|------|
| **M1 默认目录** | 飞书管理员私聊；主会话目录为已知路径 A | `/chat new 修复样式`（无 `-dir`） | 反馈含完整路径 A、任务摘要、SessionKey `temp_*`；temp 内任务在 A 语境生效 | 01·1、T3 |
| **M2 指定目录** | 另有有效目录 B（B≠A） | `/chat new 任务描述 -dir B` | 反馈展示 B；主会话仍为 A | 01·2 |
| **M3 无效目录** | — | `/chat new 任务 -dir /不存在的路径` | 友好错误；不创建 temp；活跃会话不变；修正后可成功 | 01·3、§八·(二)·1–2 |
| **M4 反馈完整性** | — | 默认 1 次 + 指定 2 次不同路径 | 三次均含任务确认与完整目录路径 | 01·4 |
| **M5 生命周期** | temp 已创建 | temp 内对话 → 结束 | 切换/对话/回退与现网一致；主目录仍为 A | 01·5 |
| **M6 并发 temp** | — | 连建两 temp，分别 `-dir` 不同路径 | 各自目录绑定正确，无串用 | 01·7 |
| **M7 切换展示** | 存在多个 temp | `/chat ls`；`/chat <序号>` | ls 仍 basename；切换反馈全路径与绑定一致 | §八·(二)·3 |
| **M8 帮助三要点** | — | 发送 `/help` 或查看 `/chat` 用法 | 可选 `-dir`、默认=主会话、无效不创建 | 01·8、T4 |
| **M9 边界** | 群聊或他人私聊 | 观察指令入口 | 无 `-dir` 能力变化 | 01·6 |
| **M10 主目录变更** | temp 运行中 | 主会话 `/workspace set` 改目录 | 已运行 temp 的 `workspaceDir` 不变 | 01 F5.1 |

**含空格路径提示**：若路径含空格，建议将 `-dir` 置于任务描述**之后**（04 §4 已知偏差）。

### 4.2 静态/编译检查（T5 已执行）

| 检查 | 命令/操作指针 | 期望 |
|------|---------------|------|
| TypeScript 编译 | `npx tsc --noEmit`（或项目等价命令） | 无类型错误 |
| 04-review 对照 | `04-review.md` §5 验收表 | 代码层 ✅ |
| 工作流 mkdir | `launchAgent` 非 temp 分支仍 `mkdirSync` | 04 静态 ✅ |

环境变量与凭据：**不写密钥**；飞书凭据以本地已配置为准（`LARK_*` 等）。

## 5、脚本位置与环境

| 项 | 说明 |
|----|------|
| **脚本目录** | 本期无 `auto_test/` 新增脚本 |
| **运行依赖** | Electron 应用 + `src/daemon`；飞书通道已连接；管理员私聊权限 |
| **环境变量** | 仅名称：`LARK_*`、主工作目录配置（Settings 或通道默认）；Agent 资源模式（SDK/CLI） |
| **数据准备** | 至少两个本地有效项目目录 A、B；可选一个不存在路径用于 M3 |

## 6、输出与记录规范

- 会话与本文**禁止**粘贴完整终端日志、含 token 的 JSON。
- 执行记录仅用 §7 表格：日期、环境、命令/场景 ID、结果、备注（一词结论）。
- 联调失败时区分：**操作/路径问题** vs **服务/通道问题**（记备注列）。

## 7、执行记录

| 日期 | 环境 | 命令/场景 | 结果 | 备注 |
|------|------|-----------|------|------|
| 2026-06-27 | 本地 dev | 04-review 全量 diff | 通过 | 静态契约 |
| 2026-06-27 | 本地 dev | `tsc --noEmit` | 通过 | T5 报告 |
| 2026-06-27 | 本地 dev | T1–T4 代码对照 03 验收 | 通过 | 04 §5 |
| 2026-06-27 | — | M1–M10 飞书 E2E | 待执行 | 需飞书管理员 |
| 2026-06-27 | — | §八·(二)·4–5 SDK/CLI/工作流 | 待执行 | 需运行态 |
